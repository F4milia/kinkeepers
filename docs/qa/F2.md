# QA — F2 facilitator certification self-view

Preview URL: <filled in by whoever verifies, from the PR's Vercel comment>
Fixtures used: Renata Solis (see docs/qa/FIXTURES.md's "Signing in as a fixture" section for how to actually sign in as her - the only fixture with a real login today)

## Primary check
A facilitator can see their own certifications - current, expiring soon, and expired - with no way to add/edit them (that stays admin-only).

1. Sign in as `ferenz+kinkeepers@brandlamb.com` (magic link via Mailpit locally, or a real inbox on production - see FIXTURES.md's "Signing in as a fixture").
   **Expect:** lands on `/facilitator` (Home), with "Certifications" now visible as a third nav item alongside Home and Schedule.
2. Click "Certifications."
   **Expect:** three rows, one per seeded certification (all against Tele-Savvy, certifying body "BPC National Training Center"), badged: one "Current" (accent), one "Expires soon" (gentle, ~30 days out), one "Expired" (urgent, ~5 days past).
3. Confirm there's no add/edit control anywhere on this screen.
   **Expect:** none - certification data entry stays admin-only (`/admin/facilitators/[id]`).
4. Sign out and visit `/facilitator/certifications` directly, unauthenticated.
   **Expect:** redirected to `/sign-in`, not shown a blank or broken page.

## Regression (previous two sessions)
- [ ] X5b: `role_escalation.sql`'s cohort-isolation coverage still passes (this PR didn't touch RLS, but double-check `npx supabase test db` is still 249/249 on the preview's linked environment if that's ever checked)
- [ ] F1: the facilitator nav's existing two destinations (Home, Schedule) still work and highlight correctly as the active tab - this PR changed `facilitator-nav.tsx` to add a third item, which could have broken the active-state logic for the first two

## Result
- [ ] All pass
- Failures → issue links:
- Loom:
- Executed by / at:
