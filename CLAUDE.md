# CLAUDE.md — KinKeepers

KinKeepers runs closed peer-support cohorts for dementia caregivers, delivered over Zoom by certified lay facilitators, sold to health systems. Members are caregivers aged 50–75: often exhausted, often with presbyopia or tremor, sometimes on a tablet a family member set up. Every design decision serves that person. The candor of a closed room is the product.

## Stack — locked, do not re-litigate

Supabase (Postgres, Auth, RLS, Realtime, Storage) · Inngest (jobs) · Resend (email) · Twilio (SMS) · Zoom Workplace for Healthcare via Server-to-Server OAuth (join URLs only — NO embedded video, no Meeting SDK, no WebRTC) · pgTAP + Playwright in CI (ZeroStep was tried and doesn't work — plain Playwright, not a stopgap) · Sentry (PII-scrubbed or disabled). Analytics live in our own Postgres. Auth is Supabase Auth; Twilio/Resend are delivery only.

## Hard invariants — violating any of these is a failed session

1. NO passwords. Anywhere. Not optional, not fallback. Magic link + SMS code.
2. NO health information in any outbound message (email, SMS, push). "Your KinKeepers sign-in code is 123456" — nothing about dementia, caregiving, the program, or the group. Assume every inbox and phone is shared with the person being cared for.
3. NO third-party analytics. No Mixpanel/Amplitude/PostHog/Segment/GA. Participation in this program is health information about identifiable people. Events stay in Postgres, queried with SQL.
4. Partner staff are STRUCTURALLY incapable of reaching post/discussion content. Not filtered, not summarized, not sentiment-scored — no code path exists. Verified by grep and by RLS test.
5. NO auto-matcher, no risk scores, no severity levels, no sentiment analysis, no engagement scoring. Absence counts only. We are peer support, not clinical monitoring. Matching is human judgment — surface signal, never decide.
6. Recording is NEVER possible. Zoom meetings carry all five enforced settings at creation: auto_recording none, waiting_room true, password required, join_before_host false, screen share host-only.
7. Attendance is PRE-FILLED, never auto-committed. A facilitator confirms. Attendance feeds payouts and delivery evidence. Store the raw Zoom report alongside the confirmation.
8. NEVER collect: diagnosis details, medications, the care recipient's name, any date of birth, any beneficiary identifier. partner_reference_id is opaque — store, never parse, never show to members, echo only in the partner export.
9. Every mutation writes to audit_log. If the audit write fails, the mutation fails. Role and cohort resolve server-side from the database, never from a client claim. RLS is the security model, not a layer on it.
10. NO hardcoded session counts or program facts — the programs row drives everything. Only license_status = 'licensed' programs are selectable. program_sessions titles/descriptions stay NULL until Ivan confirms the license permits display; never invent them.
11. Payout release is never automatic. Screens show; a reviewer releases.
12. No gamification, badges, certificates, streaks, or celebration screens. Finishing a dementia caregiving program is not an achievement to congratulate — the situation hasn't improved.

## Standing workflow — applies to every session, no paste required

1. FIRST OUTPUT, before any code: a PR plan — ordered PRs, each under 200 lines, each independently mergeable and green on its own. Then execute PR by PR. If the plan changes mid-session, restate the rest.
2. Per PR: tests first against the acceptance criteria — implement — run — self-correct until green — open the PR.
3. Migrations ship as the smallest possible standalone PR.
4. Touch only files in this session's scope. Needing a file outside it — especially migrations, auth, RLS, Zoom settings, another session's surface — means STOP and report which file and why. Do not proceed.
5. Every PR description lists: (a) every file modified, (b) any acceptance criterion not satisfied and why, (c) every assumption the prompt didn't specify.
6. Never delete or weaken an existing test to make a change pass.
7. Before opening the PR, create `docs/qa/<SESSION_ID>.md` from `docs/qa/TEMPLATE.md` (Ivan's qa-previous-session-sop.md, adapted for this repo — see that doc's own note on why it departs from ZeroStep). Expand this session's line from the run doc's "Named edge-case register" into numbered steps with concrete expected results, referencing only fixtures already listed in `docs/qa/FIXTURES.md` — add a fixture there in the same PR if a step needs one that doesn't exist yet, rather than inventing an ad-hoc one. Add a "Regression" section naming the 2-3 behaviors from the previous two sessions most likely disturbed by this change. Cap at 15 steps. A manual step that has passed QA twice in a row graduates into a Playwright test in `e2e/` instead of staying a manual step forever.

## Design constraints — every member/facilitator screen

18px base text · 48px touch targets (56px primary actions) · WCAG AAA contrast · keyboard operable · no time-based interactions · no skeleton shimmer (plain "Loading…") · no invented copy · plain and warm, never cheerful, no emoji, no exclamation points. The human phone number appears in every error state and every locked-out moment — this population's fallback is a person, not a refresh. Error states never dead-end. The /admin density exemption applies to /admin only. New UI strings go in the copy deck (frontend-build Part 3.1), never inline.

## Testing rules

RLS tests authenticate as real users with their own JWTs — NEVER the service role key, which bypasses RLS and makes the test worthless while passing. Every isolation test must demonstrably fail with its policy removed (write — delete policy — watch fail — restore). Failing isolation tests block merge, no override.

## Architecture notes for future sessions

Implementation-specific conventions already decided in this codebase — not covered by any companion doc below, so kept here rather than lost when this file was consolidated from the earlier frontend-only CLAUDE.md.

- See `@AGENTS.md` for framework-version notes — auto-managed by `next dev`, do not hand-edit.
- **Tailwind v4, CSS-first config.** There is no `tailwind.config.ts` — all tokens live in
  `app/globals.css` via `@theme` / `@theme inline` blocks. Color and type tokens use
  `@theme inline` because they reference runtime custom properties (the `.dark` class, the
  768px media query); static tokens (radius, spacing, container width) use plain `@theme`.
  If you need a new token, add the CSS custom property and its `@theme` mapping in
  `globals.css` — don't reach for a JS config file.
- **Dark mode**: class strategy (`.dark` on `<html>`), persisted via the `kk_theme` cookie (not
  localStorage — see `lib/theme.ts`). On first visit with no cookie, a small blocking inline
  script in `<head>` (`themeInitScript`) reads `prefers-color-scheme`, applies the class before
  paint, and writes the cookie so subsequent requests render server-side with no flash.
  `app/layout.tsx` reads the cookie server-side via `next/headers` and sets the class directly on
  first paint whenever a cookie already exists. `components/theme-toggle.tsx` is the only place
  that writes the cookie after initial load.
- **Data layer**: `lib/fixtures/` holds typed mock data (`Cohort`, `CohortMember`, `Facilitator`,
  `Session`, `Post`) populated verbatim from Part 3.2 of the build doc. `lib/data.ts` is what
  screens actually import (`getCohort`, `getCohortMembers`, `getFacilitator`, `getSessions`,
  `getPosts`, `getUpcomingSession`) — when a real API exists, only `lib/data.ts` changes (L5,
  Wave 8).
  Session dates in the fixtures are concrete ISO dates anchored to a real Tuesday, not the
  relative labels ("next Tuesday") used in the prose of the build doc.
- Tele-Savvy session topics come from licensed curriculum materials and are not cleared for
  display — `Session.topic` stays in the type but is `null` in every fixture. Do not invent
  session titles.
- `program`, `sessionTotal`, and `deliveryFormat` are fields on `Cohort`/`Session`, never
  hardcoded assumptions — other programs (Stress-Busting: 9 sessions, Powerful Tools: 6) will run
  on this same platform.
- No photographs of people anywhere, including avatars — initials only, deterministic per member.
- **Supabase default ACLs are permissive, not restrictive.** New tables and functions get FULL
  privileges (INSERT/SELECT/UPDATE/DELETE/TRUNCATE, EXECUTE) granted to anon/authenticated/
  service_role automatically at creation time — confirmed via `pg_default_acl` on both local and
  the hosted project. A GRANT statement in a migration is additive on top of this, never a
  starting point: omitting a GRANT restricts nothing. Every restriction must be an explicit
  REVOKE. This bit both P1 (sign_in_events/profiles, fixed in p1-harden-grants) and P7a
  (audit_log) independently before either stream knew to look for it.
- **The local Supabase stack is shared across worktrees, not per-worktree.** `supabase start` /
  `supabase db reset` operate on Docker containers named after `project_id` in
  `supabase/config.toml` ("Kinkeepers") — every worktree of this repo on the same machine talks
  to the same containers. Whoever runs `db:reset` needs the full current
  `supabase/migrations/` directory, not just their own new files, or it silently drops schema
  the other stream depends on.
- **Production (`vnadfnnckmkswfrzfjkj`, cut over as part of R1) never receives test data —
  prevention, not cleanup.** The old hosted project (`lupiicjafzrbihaosezv`) is now staging only,
  wired to Vercel's Preview environment; production is wired to Production only, and the two are
  separate databases a code deploy never moves data between. Any manual test data for a fix or
  improvement (a test cohort, a backdated session, a throwaway applicant) always targets staging
  or local — never production directly — and whoever is about to write test data states which
  environment it's headed for before doing it. This is the guarantee that production stays clean
  for real cohorts: there being nothing to delete before launch, not a promise to remember to
  delete it after. Confirmed with Ferenz (2026-09-03) as the standing rule for both streams
  post-cutover.

## Companion docs

kinkeepers-frontend-build.md (design system, copy deck) · KINKEEPERS-COMPLETE-RUN-DOC.md (waves, session prompts, edge-case register) · kinkeepers-testing-workflow.md (review pipeline, trigger globs, the 09:30 merge window) · f4milia-master-sequence.md (program data model notes) · f4milia-revenue-model.md §2 (facilitator compensation).

**`f4milia-master-sequence.md` is missing** - referenced above and nowhere in this repo's git history (checked 2026-09-04, all branches). `f4milia-revenue-model.md` had the same problem (present only on a stale, never-merged local branch `docs/f4milia-companion-docs`, based on an Aug 26 commit) until recovered onto `main` the same day - see that PR for the doc's own content, which describes the *separate* F4milia/Trib4l platform's mentor-payout infrastructure (Stripe Connect splits, `mentor_agreements`/`mentor_payouts`) that A4-payouts and F3 are parked on (the run doc's "B1"/"B3"). Whoever can locate `f4milia-master-sequence.md` should add it the same way - a referenced companion doc that doesn't exist is a standing risk of scope drift (see the 2026-09-03 "Real run doc reconciliation" entry below for what that already cost once).

## Learned constraints — append-only; never edit or remove entries

Format: `YYYY-MM-DD · session · what happened · the rule now`. Every PR tagged `rework` adds a line here before the next session launches. Every discovered hidden coupling or non-obvious constraint adds a line, rework or not. This section is why week four is smarter than week one.

- 2026-08-26 · (seed) · Zoom participant reports identify phone joiners by number, not name · phone-join attendance requires E.164 matching (X4); never guess partial matches — ambiguous callers surface to the facilitator.
- 2026-08-26 · (seed) · staging must never send real messages · Twilio/Resend test mode or team-only allowlist; verify via provider logs, not assumption.
- 2026-08-26 · P1/P7a (both streams, independently) · Supabase's default ACLs grant new tables and functions FULL privileges to anon/authenticated/service_role at creation time — a GRANT in a migration is additive on top of this, not a starting point · every restriction needs an explicit REVOKE; see Architecture notes above.
- 2026-08-26 · P1 (Stream A) · Supabase's Management API `sessions_inactivity_timeout` field expects HOURS, not seconds — a value intended as "90 days in seconds" (7776000) was silently accepted, then crash-looped GoTrue ("invalid duration 7776000h") on the next full restart, causing a ~15-20 min Auth outage on the hosted project · convert to hours explicitly (2160 for 90 days) before writing this field via the Management API; GoTrue only re-reads it on a full restart, so a bad value can sit dormant and pass review before it actually breaks anything.
- 2026-08-27 · P7a (caught by Stream A) · audit_log.actor_id references profiles(id) with no ON DELETE behavior (defaults to RESTRICT), and audit_log is append-only by design — once a profile has written one audit row, that profile can never be hard-deleted, ever · P6/A5's deletion-fulfillment flow must anonymize/detach the profile rather than DELETE it; do not assume a plain delete works once a member has any privileged-action history (e.g. was ever a facilitator or admin). Also broke an absolute-row-count pgTAP assertion in audit_log.sql once a second test suite (admin-issued-sign-in-link) started writing real permanent rows - fixed to a baseline-delta assertion; any future audit_log test needs the same pattern, never an absolute count.
- 2026-08-27 · Stream A (E2E infra) · ZeroStep does not work, tried directly · dropped permanently, not a stopgap. E2E is plain Playwright (playwright.config.ts, e2e/*.spec.ts, path-filtered CI via .github/workflows/e2e.yml on app/**, components/**). Do not attempt to wire ZeroStep back in.
- 2026-08-28 · A1 PR3 (Stream A) · a pgTAP suite for two new audit_log-writing functions used an absolute-count assertion, reasoned as safe because the two audit_action enum values were brand new in that same migration and "nothing could have written them yet" - true only until a vitest integration test (lib/admin/partner-organizations.test.ts) that calls the same functions against the real, non-rolled-back local database ran once and left permanent rows behind, since audit_log is append-only and pgTAP's own begin/rollback wrapper doesn't protect against writes made outside that transaction by a different test runner · any pgTAP assertion touching audit_log needs the baseline-delta pattern (see audit_log.sql), never an absolute count - "this enum value is brand new" is not an exemption, because app-level integration tests (vitest, not just other pgTAP suites) can and do write real committed rows through the same code path before the migration's own PR even merges.
- 2026-08-29 · L4 (Stream B) · a stacked PR (#51, based on #50's branch) showed as "Merged" on GitHub, but its content never reached `main` - the reviewer merged #50 (branch → main) before merging #51 (PR2's commit → #50's branch), so `main` only picked up whatever #50's branch contained at the moment IT was merged, not the later commit #51 added to that same branch afterward. GitHub's PR "Merged" badge reflects the commit landing in the PR's own base branch, not in `main` - true only for a stacked PR. Caught by `git merge-base --is-ancestor <merge-commit> origin/main` returning false despite both PRs reading "Merged". · for a stacked PR chain, the base PR must merge to `main` LAST, after every PR stacked on it has merged into it first - merging the base PR early silently drops everything stacked on top, with no error and no visual sign on either PR. If merge order gets reversed anyway, the fix is a clean cherry-pick of the stranded commit onto current `main` in a fresh PR - the work isn't lost, just unreachable, and recovering it must not replay the stale branch wholesale (it will show every file the base stream has since gained as a deletion).
- 2026-08-31 · A3 (Stream A) · the exact L4 failure mode above recurred on a 3-deep stack: PR64 (`a3-cohort-schema` → `main`) merged 15 seconds *before* PR65 (`a3-cohort-creation-action` → `a3-cohort-schema`) finished, so `main` got only PR1's schema - PR2's orchestration functions and PR3's admin screen never reached `main` despite all three PRs reading "Merged". All three merges landed within the same 12-second window, meaning even a careful reviewer clicking "Merge" in the visually-correct bottom-to-top order can lose the race if GitHub's own merge-into-base and merge-into-main requests overlap - this is not solely a discipline problem, it can happen even when the merge button clicks happen in the right order. Recovered by creating a fresh integration branch off the stack's tip, `git merge origin/main` into it (a real 3-way merge, not a rebase/cherry-pick), confirmed the merge alone restored every unrelated file (another stream's parallel PR) that a naive tip-to-tip `git diff` had shown as a deletion, then opened one landing PR of the reconciled branch into `main` · for any stack 3+ PRs deep, verify each intermediate merge actually landed (`git log --oneline origin/main..origin/<next-branch-in-stack>` should be empty right before merging the next one) before clicking merge on the next PR in the chain - do not trust merge-order alone once three or more merges are happening close together in time. When recovery is needed, build the landing branch from a real `git merge`, never a diff-and-replay, so parallel unrelated work merged to `main` in the meantime is preserved rather than showing as deleted.
- 2026-09-01 · A3 (Stream A) · the SAME failure mode recurred a THIRD time on the recovery PR itself: the landing PR (#67, `a3-landing-main` → `main`) merged at 16:13:53, then the very next PR stacked on it (#68, session management) merged into #67's branch 12 seconds later at 16:14:05, and the PR stacked on #68 (#69, cohort completion) merged even later at 16:17:13 - so `main` again only picked up what the base branch contained at the moment it merged, silently dropping PR4 and PR5 despite both reading "Merged." This was caught only because a user's manual test of a not-yet-verified feature turned up a runtime error that shouldn't have been possible if the code were really live - a reminder that "the PR merged" is never sufficient confirmation on its own. Recovered the same way (fresh branch off the stack's tip, real `git merge origin/main`, one new landing PR - see the entry directly above for the full method) · treat every PR in a 3+-deep stack as suspect, including a PR whose entire purpose was fixing a previous instance of this bug - the fix is not immune to the bug it was fixing. After ANY stack of 3+ PRs reports as fully merged, the closing step is non-negotiable: run `git log --oneline origin/main..origin/<tip-branch>` and confirm it is empty before treating the feature as live, telling a user it shipped, or moving on to build the next thing on top of it.
- 2026-09-01 · A3 (Stream A) · `sessions.video_occurrence_id` was added by editing an EXISTING migration file (`20260829180000_cohort_creation_schema.sql`) with a new commit on its still-open PR branch, rather than writing a new migration file - reasoned as safe at the time because the PR hadn't merged yet. It merged into `main` in between the edit's two halves, and separately, its ORIGINAL (pre-edit) content had already been pushed to the hosted project via `supabase db push` before the edit was even written. Supabase's migration tracking records applied migrations by filename/version, not by content hash - so the later push of the edited file was silently a no-op on hosted (that version was already marked applied), and the column simply never existed there. Every local/CI environment looked fine because `supabase db reset --local` rebuilds the database from scratch, replaying every migration file's CURRENT content in order - it can never reveal this kind of drift, only a real diff against a persistent database can. Surfaced by a user's manual test failing with a misleading "Session not found" (the actual Postgres error - a missing column - was being swallowed and relabeled by `lib/admin/session-management.ts`'s own error handling); confirmed and scoped precisely via `supabase db diff --linked`, which flagged the missing column as the only real drift · a migration file must never be edited again once ANY environment may have applied it - not just once its PR merges to `main`, but from the moment `supabase db push`/`migration up` has been run against hosted OR any shared persistent database, including mid-PR-review pushes for manual testing. A schema change discovered to be needed after that point is always a new migration file, even if the "original" migration technically hasn't merged yet. When drift is suspected between local and hosted, `supabase db diff --linked` finds the exact discrepancy directly - it does not require guessing from symptoms. Separately: don't let a data-access helper collapse a real query error into a generic domain message ("Session not found") - surface the actual error, or the true cause (missing column, wrong permissions, whatever it is) gets hidden behind a misleading one.
- 2026-09-01 · P4/A5 verification (Stream A) · a user testing a real reschedule/notification on the live site got no email and no matching log line at all - not a code bug, but the Vercel `kinkeepers` project's GitHub connection was silently non-functional: `vercel project inspect` showed no Git section, and the Vercel API's project `link` object had `"sourceless": true` alongside a correctly-set `"productionBranch": "main"`. That flag means Vercel holds a record of which repo it's supposed to watch but was never actually subscribed to its push/webhook events - so merges to `main` never triggered a build, ever, and every deployment for this project (many, over the session) had instead been a manual `vercel --prod` run from whichever local checkout happened to invoke it, completely disconnected from actual git state. The live site had been stuck around A3-era code for the entirety of A5 and P4's development despite both being merged to `main` long before. Confirmed precisely via `curl` against known routes (`/api/inngest`, `/admin/data-requests` - both 404 on the stale deployment, both 200 after a manual redeploy from a fresh `origin/main` checkout) and via the Vercel REST API's `link.sourceless` field (`vercel project inspect`'s own CLI output doesn't surface this flag at all) · "merged to main" and "live on Vercel" are two independently-failing claims for this project, not one - verifying a feature is actually deployed requires checking the deployed site itself (curl a route only that feature adds), not just `git merge-base` against `main`. If a real send/feature mysteriously produces zero log output for a request that definitely happened, suspect the deployed code is simply older than the feature, before suspecting the feature's own logic. Fixed by disconnecting and reconnecting the GitHub integration in the Vercel dashboard (Settings → Git), which re-runs the GitHub App authorization flow neither the CLI nor the API can complete unattended.
- 2026-09-02 · L5 (Stream A) · confirmed by hand while writing pgTAP negative-test drills for two new `SECURITY DEFINER` functions (`claim_applicant_for_current_user`, `list_cohort_roster`) that the "Supabase default ACLs are permissive, not restrictive" finding (2026-08-26 entry, above) applies to function EXECUTE privileges exactly as it does to table grants, not just tables as originally scoped - commenting out each function's own `grant execute ... to authenticated` line and resetting made no test fail at all, because the default ACL already grants EXECUTE to anon/authenticated/service_role at function-creation time regardless of that line; commenting out the corresponding `revoke execute ... from public, anon` line instead did fail the anon-cannot-call assertion every time (once with `not_authenticated` raised instead of a permission error, once with zero rows and no exception at all) · for a new `SECURITY DEFINER` function meant to be authenticated-only, the `grant execute ... to authenticated` line is cosmetic and the explicit `revoke execute ... from public, anon` is the only real enforcement - write the negative-test drill against the revoke line, not the grant line, or the drill will falsely "pass" while proving nothing.
- 2026-09-02 · P5/X4 (Stream A) · two P5 functions (`attendance_rate_by_session_number`, `retention_at_session()`) computed "did this person attend" from the mere EXISTENCE of a `session_attended` event, reasoned as safe because nothing wrote a second, contradicting event for the same person - true only until X4's `submit_session_log()` landed, which fires a NEW event only when a status actually changes, leaving a superseded event (e.g. an original `session_attended` later corrected to `session_missed`) permanently sitting in the append-only `analytics_events` log alongside the new one. Both functions kept counting the stale original, so a member corrected from present to excused still showed as "attended." Writing the pgTAP regression test for the fix surfaced a second, independent gotcha: the first fix attempt broke the tie between the stale and current event using `order by occurred_at desc`, which failed unpredictably in the pgTAP suite itself because Postgres's `now()` is frozen for the entire duration of a transaction - two events recorded moments apart inside the same `begin`/`rollback`-wrapped test get an IDENTICAL `occurred_at`, so the tie-break could resolve to either row · any "most recent event for X" query must resolve ties by the row's own monotonic `id` (bigint identity), never `occurred_at`/`now()` - a timestamp column cannot reliably order events created within one transaction, which is exactly how every pgTAP suite in this repo runs. More generally: an aggregate reasoned as safe because "nothing currently writes a second event for the same key" needs re-verification the moment ANY new code path can write a correction/update event for a key already seen - the aggregate's own correctness assumption doesn't announce itself as broken, it just quietly starts returning stale answers.
- 2026-09-02 · X4 manual verification (Stream A) · a real facilitator sign-in on the live site landed on "We couldn't find that" twice in a row, discovered only because Ferenz was manually testing X4's dial-in acceptance criteria and needed to sign in as a facilitator to reach the session-log screen. Root cause: `app/auth/callback/route.ts` always redirected to `next ?? "/"` after a successful magic-link exchange, and no caller of `signInWithOtp`/`requestEmailLink` has ever actually passed a `next` param - so every sign-in, regardless of role, landed on "/", the caregiver home page. That page's `getViewer()` calls `notFound()` when the signed-in user has no `applicants` row, which is exactly the shape of a facilitator or admin account - so the sign-in itself was succeeding (the session cookie was set) but the very next screen 404'd, making a fully-working auth flow look broken. Fixed by resolving the destination from the signed-in user's own role (`lib/auth/roles.ts`'s new `roleHomePath()`, fed by the already-server-side `resolveUserAndRole()`) rather than a fixed default, with `next` kept only as an explicit override · dead-looking plumbing (an unused query param with a hardcoded fallback) is not proof the fallback is safe for every caller - a route's default redirect target needs to be checked against every role that can legitimately land there, not just the role whose flow was being tested when the redirect was written. More generally: when a real user reports a screen behaving like the wrong page loaded, check whether a shared post-auth/post-action redirect is defaulting to a destination that assumes a role or record the current actor doesn't have, before assuming the underlying action itself failed.
- 2026-09-02 · P3 manual verification (Stream A) · submitting a real applicant through the live intake form and then opening their admin assignment page (`/admin/applicants/[id]`) crashed the entire page with `RangeError: Invalid time zone specified: Central`, caught by Vercel logs, not by any test. Root cause: `components/referral/intake-form.tsx`'s `TIME_ZONE_OPTIONS` used `{ value: "Eastern", label: "Eastern" }`-style friendly region names as the actual submitted `value` - stored verbatim into `applicants.time_zone` by `lib/referral/actions.ts` with zero transformation - while every other consumer in the codebase (`lib/admin/cohort-meeting-time.ts`'s `formatMeetingTime`, `lib/session-time.ts`) feeds that column straight into `Intl.DateTimeFormat`, which requires a real IANA identifier and throws on a bare region name. `lib/session-time.ts`'s own header comment even asserted "L2's intake form... only offer[s] these [IANA zones]" - a stale claim nothing had checked against the actual form code, which had drifted from it. Every other time-zone value in the codebase (cohorts, sessions, fixtures, tests) was already a correct IANA identifier - this was the one place that wasn't, and it went unnoticed because no automated test exercises the real intake form's stored value against a downstream `Intl.DateTimeFormat` call. Fixed by changing the dropdown's `value`s to real IANA identifiers (`America/New_York` etc.) while keeping the same friendly `label`s, matching `lib/session-time.ts`'s existing `FRIENDLY_ZONE_LABELS` mapping; two already-created test applicants with the bad literal values were repaired directly in the hosted DB · a comment asserting "every caller of X already does Y correctly" is a claim about the CURRENT state of other files, not a guarantee - it can go stale the moment any of those files changes, and nothing re-checks it. When a value is stored by one form/component and consumed by an entirely different one many files away (especially something passed to a strict runtime API like `Intl.DateTimeFormat`), grep for every other place that same conceptual value is produced (here: every other `time_zone` assignment in the codebase) and confirm the new code actually matches that shape - don't trust a nearby comment's word for it.
- 2026-09-03 · A5 gap-closure (Stream A) · re-verifying A5's own original acceptance criteria against the real, current codebase found that 2 of its 6 items were never actually built: "unlogged past sessions surface" and "the two-consecutive-absence flag is accurate." `app/admin/reports/page.tsx`'s own header comment explained why - it was written and shipped in Wave 7, before X4 (Wave 9) created `session_logs`/`session_attendance` at all, and said outright that attendance summaries were "explicitly out of scope... there is no attendance table yet." Once X4 landed weeks later, nothing ever came back to build the two items that comment had deferred - the gap was invisible because the page still rendered fine (it just quietly never grew the sections A5's spec asked for), and no test asserted their absence was temporary. Closed by adding `getUnloggedPastSessions()`/`getConsecutiveAbsenceFlags()` to `lib/admin/reports.ts` and two new sections on the same page. Also found in the process, and left as a separate still-open gap: A5's third criterion ("attendance corrections preserve prior values and write audit rows") IS correctly proven at the DB/pgTAP level by X4's `submit_session_log()`, but `lib/admin/audit-log.ts` never selects the `metadata` column those rows carry, so the admin browser UI still can't show a human the before/after values - the write path being correct doesn't mean the criterion's "visible" half is satisfied · a session's own explicit "out of scope, dependency X doesn't exist yet" comment is a snapshot of that moment, not a standing invariant - it does not self-update once the dependency it named actually ships, and nothing in this codebase's workflow currently re-checks a deferred scope note against the feature it was waiting on. When a later session builds the dependency a prior comment was blocked on, grep for that prior session's own code/comments referencing the new capability - a shipped prerequisite does not retroactively finish the work that was waiting on it.
- 2026-09-03 · Real run doc reconciliation (Stream A) · Ferenz shared `KINKEEPERS-COMPLETE-RUN-DOC.md` (the actual session prompts) mid-session, which had never been checked into the repo despite CLAUDE.md's own pre-flight step listing it as a required companion doc - both streams had been reconstructing session scope from fragments/memory all session (e.g. X4's own migration comment says "confirmed with Ferenz" for a scope question the real doc had already answered). Cross-referencing it against the actual codebase surfaced two more real, previously-unknown gaps in the same missed-because-it-shipped-before-its-dependency shape as the A5 entry above: (1) P3's own prompt specifies a `video_provider` column plus per-cohort Zoom credentials ("a cohort may carry its own Zoom credentials... a cohort with its own Zoom credentials uses them" is P3's own acceptance line) - `video_provider` does not exist anywhere in the schema, so this was never built, not a new feature invented later; (2) A5's real acceptance criteria has a line a reconstructed/truncated version of it (being manually tested from memory earlier this session) omitted entirely - "Partner export scoped correctly and carrying partner_reference_id... Export stays labeled 'Export attendance and delivery (CSV)'" - and no CSV export existed anywhere on `/admin/reports`, only a plain HTML list. Also confirmed a genuine spec-vs-reality mismatch, not a build gap: P3's prompt assumes "screen share: host only" can be "set via API" at meeting creation, but Zoom's own developer forum confirms this is an account/user-level default only, never a per-meeting Create Meeting field. Fixed the CSV gap in the same session (`lib/csv.ts`, `getPartnerAttendanceExportRows()`, `/admin/reports/export`, plus the missing `session_attendance_select_own_partner_referrals` RLS policy the export needed - `session_attendance` didn't exist when `20260901150000_partner_cohort_scoping.sql` added the equivalent cohorts/sessions policies, so it was never extended to the third table once X4 built it) · a session's own acceptance criteria, quoted from memory or a prior summary rather than the original source document, can silently drop lines - always re-verify a "here's what X's acceptance criteria says" claim against the actual source prompt before treating an omission as evidence a feature doesn't need to exist. More generally: a companion doc a project's own governance file says should be in the repo, but isn't, is a standing risk of exactly this kind of compounding scope drift across both streams - get it into the repo the first time its absence is noticed, not the third.
- 2026-09-03 · A5 manual verification (Stream A) · `/admin/reports` crashed in real production the moment it actually had an unlogged session to render - `RangeError`-adjacent `TypeError: Invalid option : option` from `new Intl.DateTimeFormat("en-US", { dateStyle: "medium", timeStyle: "short", timeZoneName: "short" })`. This combination is invalid per Intl's own spec (`dateStyle`/`timeStyle` cannot be mixed with a component option like `timeZoneName`), but it shipped in PR #109 anyway because every test that PR ran (`vitest`, `tsc`, `eslint`) exercises the DATA layer (`getUnloggedPastSessions()` returning the right rows), never the actual `.map()` render call with a non-empty array - and hosted had zero real unlogged sessions until this session's own manual-testing setup (backdating a session's `scheduled_at` to create one) made that array non-empty for the first time in production. Some browsers are lenient about the same combination and won't reproduce it - this is specifically a Node/V8-ICU strictness difference, so a developer manually clicking through the same screen in Chrome during review would not have caught it either. Fixed by using explicit date/time component options instead of `dateStyle`/`timeStyle`, moved to `lib/format-date.ts` (a plain module, not `lib/admin/reports.ts` - that file has `"use server"` at the top, and Next.js requires every export from a `"use server"` file to be an async function, so a synchronous formatter can't live there) with a real regression test that calls the actual `Intl.DateTimeFormat` · a data-fetching function returning the right rows in a unit test is not the same claim as "the page that maps over those rows renders correctly" - when a new UI section's underlying data is empty in every environment a PR is tested against (no real rows exist to populate it yet), the render path for a NON-empty case is genuinely untested no matter how green the test suite is, and stays that way until real data exists - which may be long after merge. For any `Intl.DateTimeFormat` call, never combine `dateStyle`/`timeStyle` with a component option (`timeZoneName`, `weekday`, `hour`, etc.) - pick one style: either the two style shorthands alone, or explicit components alone.
- 2026-09-03 · X4/F1 manual verification (Stream A) · manually testing A5's "unlogged past sessions" fix, two sessions were backdated to a real past `scheduled_at` (still `status: 'scheduled'` in the DB, since nothing ever transitions that column - see the Learned Constraints entries above for A5). They correctly appeared on `/admin/reports`, but NEVER appeared in the facilitator's own Home "Needs a log" section, which uses a completely different, older function: `getFacilitatorSessionsNeedingLog()` filters `session.status === "past"`, where `Session.status` (the app-level derived field, distinct from the DB's raw enum) came from `mapSessionStatus()` in `lib/data.ts` - `return status === "scheduled" ? "upcoming" : "past"`. Since the DB's `status` column NEVER actually transitions away from `'scheduled'` anywhere in this codebase (confirmed repeatedly this session), this function mapped EVERY real session to `"upcoming"` forever, regardless of how long ago `scheduled_at` had passed - a session that happened a year ago would still read as the member's or facilitator's "upcoming" one. This silently broke `getUpcomingSession()` (a member's Home would show session 1 as "next" forever, never advancing), `getFacilitatorSessionsNeedingLog()` (would never surface anything), and `fetchSessionLogDetails()` (only fetches for a `"past"` session, so real logged attendance was never actually read back for display). Existing tests never caught this because every one of them seeds a literal `status: 'completed'` row directly via the admin client to represent "a past session" - a condition that has never once occurred on a real row in this app, since nothing writes it. Fixed by deriving `"past"` from `scheduled_at` vs `now()` when the DB status is `'scheduled'` (non-`'scheduled'` statuses still map to `"past"` unconditionally, unchanged), with a new regression test that seeds the ONLY shape a real row ever has (`status: 'scheduled'`, past `scheduled_at`) rather than the artificial `'completed'` shortcut · a test fixture that sets a DB enum value to represent a real-world condition is only valid coverage if something in the actual app ever writes that same value under that condition - when nothing does (verify by grep, as this session did three separate times for `'completed'`), the fixture is testing a state that can never occur in production, and the code path meant to handle the REAL condition (time-based, not enum-based) can be completely broken while every test stays green. When investigating "X never shows up where it should," check whether the screen that DOES work and the screen that DOESN'T are actually reading from the same underlying function - two features that sound like the same concept (A5's admin oversight list vs F1's facilitator home list) had completely independent, differently-buggy implementations of "is this session in the past."
- 2026-09-03 · P3 gap-closure, video_provider/partner Zoom credentials (Stream B) · two real, hand-verified mistakes while adding `finalize_cohort_sessions`'s new `p_video_provider` parameter and a new `partner_zoom_credentials` table. First: `CREATE OR REPLACE FUNCTION` alone is NOT sufficient to add a new parameter to an already-applied function, even one with a default - Postgres identifies a function by its (name, parameter TYPE list), so a 9-parameter original and a 10-parameter "replacement" are two DIFFERENT signatures, and `CREATE OR REPLACE` silently created a second, overloaded function instead of replacing the first. Every call then failed with a real `42725: function ... is not unique` error, caught immediately by the full pgTAP suite (`cohort_creation_functions.sql`, 9 of 15 assertions failed) - not assumed, not missed. Fixed with an explicit `drop function if exists finalize_cohort_sessions(<the exact original 9 argument types>)` before the `create function` with the new 10-parameter signature; the drop also required re-stating the function's `revoke`/`grant` lines, since `DROP FUNCTION` removes any privileges that existed on the old signature and a genuinely new function object gets Supabase's own permissive default ACL again. Second, independently: on a brand-new table (`partner_zoom_credentials`), the negative-test drill's FIRST attempt (commenting out `grant select/insert/update/delete ... to service_role`) found nothing - all tests still passed, because `service_role` already holds full privileges on any new table via Supabase's own default ACL, and this migration never revoked them (only from anon/authenticated). Commenting out the `revoke all ... from anon, authenticated` line instead was what actually broke the isolation (both anon and authenticated could suddenly read the table) - the SAME "the grant to service_role is cosmetic, the revoke from anon/authenticated is what matters" shape the 2026-09-02 L5 entry already found for function EXECUTE grants, now confirmed to apply identically to table-level DML grants, not just EXECUTE · (1) adding a parameter to an existing, already-applied `SECURITY DEFINER` function needs an explicit `DROP FUNCTION` naming the OLD exact signature first, never a bare `CREATE OR REPLACE` - and re-state its grants afterward, since the drop wipes them; verify with `supabase db reset --local` + the full pgTAP suite, which will show "is not unique" immediately if this is skipped. (2) a negative-test drill on a new table's `grant ... to service_role` line will misleadingly "pass" even when broken, exactly like the function-EXECUTE case - always drill the `revoke ... from anon, authenticated` line specifically to prove a service-role-only table is actually isolated, and don't stop after the first drill attempt "passes" without checking which line it actually disabled.
- 2026-09-03 · Polish pass (Stream A) · `supabase/tests/database/session_attendance.sql`'s own regression assertion for the P5 stale-event bug (`attendance_rate_by_session_number... = 0`) intermittently failed - not Docker flakiness as first assumed (two earlier sessions this same day had blamed a genuinely flaky `vector` container for what turned out to be a different bug each time; this is the third, real cause). Root cause, confirmed by direct reproduction: `attendance_rate_by_session_number` is a deliberately GLOBAL, cross-cohort aggregate (grouped only by `session_number`, no cohort filter - P5's own intent, "where in the program people drop"), and the test asserted an ABSOLUTE value (`= 0`) against it - exactly the fragile pattern the 2026-08-28 audit_log entry already named. Running `npx vitest run` (which makes real, COMMITTED writes via the actual app code path, never rolled back like a pgTAP transaction) before `npx supabase test db`, with no `db reset --local` in between, leaves real `session_attended`/`session_missed` rows in the shared local database from unrelated vitest integration tests - confirmed directly: exactly one real leftover `session_attended` row for `session_number = 1` was sitting in `analytics_events` after a normal `vitest run`, which the pgTAP test's own absolute-zero assertion had no way to tolerate. Fixed with the same baseline-delta pattern already used for `audit_baseline` in the same file: capture `attendance_rate_by_session_number`'s count for session_number 1 BEFORE this test's own fixture submits anything, then assert the delta returns to zero after the correction, not that the raw value is zero. Verified by deliberately reproducing the polluting sequence (reset → vitest run → pgTAP on this file alone, no reset in between) and confirming the fixed assertion now passes despite a real leftover row being present · the 2026-08-28 audit_log lesson - "any assertion against a shared, cross-test, GLOBAL table needs a baseline-delta, never an absolute count" - applies to every such table in this codebase, not just `audit_log`; `analytics_events` is the same shape (real app-level writers commit real rows outside any test's own rollback) and needed the identical fix. Before trusting "this must be Docker/environment flakiness" for an intermittent pgTAP failure, first check whether the failing assertion is an absolute value against a table any vitest test could plausibly have written to, and try reproducing by running that exact sequence (vitest then pgTAP, no reset) deliberately rather than blaming the infrastructure.
- 2026-09-03 · UI/UX consistency pass, manual verification (Stream A) · `issueAdminSignInLink()` - P1's "required, not optional" human recovery path for a member who can't receive their own magic link/SMS - never actually established a session when redeemed, discovered only by manually generating a real admin-issued link against the hosted staging project and following it in a browser, which landed back on `/sign-in` instead of signing in. Root cause: it returned Supabase's `generateLink()` `action_link` directly, but that API call is made BY an admin ON BEHALF OF another user, which makes the PKCE flow this app's real sign-in uses (`signInWithOtp` → `/auth/callback?code=...` → `exchangeCodeForSession`) structurally impossible - PKCE requires a `code_verifier` only the ORIGINATING client can hold, and an admin acting for someone else's account can never be that client. GoTrue silently falls back to an implicit-flow-style link instead (session tokens in the URL hash fragment on redirect), which this app has no code to read anywhere (no hash-fragment parsing, no client-side `setSession()`) - so the link looked completely normal (still contained the string "magiclink", still redirected somewhere) while doing nothing. The existing test (`admin-issue-sign-in-link.test.ts`) only asserted `actionLink` contained `"magiclink"`, never followed or redeemed it, so this shipped and stayed green through every prior review. Confirmed the fix before writing any code: `generateLink()`'s response also includes `hashed_token`, redeemable server-side via `supabase.auth.verifyOtp({ token_hash, type })`, which needs no PKCE verifier at all - proved this worked with a live call against the hosted project before touching the app. Fixed by having `issueAdminSignInLink()` build its own link pointing at `/auth/callback?token_hash=...&type=magiclink` (using the app's own request-derived origin, reusing `lib/auth/actions.ts`'s existing `getRequestOrigin()` rather than duplicating its host-header logic - now exported for that reuse) and adding a second branch to `/auth/callback/route.ts` that calls `verifyOtp()` when the request carries `token_hash`+`type` instead of `code`. The test now actually redeems the link's own `token_hash` with a plain anon client and asserts a real session comes back for the right user, not just a string match · any Supabase Admin API call that generates a sign-in artifact FOR another user (not the calling client itself) cannot participate in PKCE, no matter how the app's normal sign-in flow works - it needs the `hashed_token`/`verifyOtp()` path, and the app's callback route must have a branch that accepts that shape. More generally: a test asserting a returned link/token merely CONTAINS an expected substring is not proof the link works - when a test's whole subject is "does this artifact function when actually used," the test must actually use it (follow the redirect, redeem the token, call the same verification the real consumer calls) rather than pattern-matching its shape.
- 2026-09-03 · Polish pass, test-coverage gaps (Stream A) · adding tests for `getSessionAttendancePreFillAction()` surfaced a real ordering bug while doing it: its Zoom-credentials parameter was written as `zoomCredentials: ZoomCredentials = getDefaultZoomCredentials()`, matching the exact default-parameter pattern already established elsewhere in this codebase (`getAttendancePreFill`, `createRecurringMeeting`) for testability. That pattern is safe on those OTHER functions because credentials are the first thing they need - but here, a JS default parameter value is evaluated eagerly at call time, in argument order, before the function body runs at all - so `getDefaultZoomCredentials()` (which throws if `ZOOM_ACCOUNT_ID`/`ZOOM_CLIENT_ID`/`ZOOM_CLIENT_SECRET` aren't set) fired and threw BEFORE this function's own `requireRole()` permission check, cohort-ownership check, or missing-`video_occurrence_id` check ever ran. In production this meant: a member with no facilitator/admin role hitting this action when Zoom credentials happened to be unset would see a raw "missing credentials" error instead of the intended permission rejection - a real, if minor, information/ordering leak, only found because a test tried to exercise the permission-rejection path without wanting to also configure fake Zoom credentials for it. Fixed by making the parameter genuinely optional (`zoomCredentials?: ZoomCredentials`, no default value) and resolving `getDefaultZoomCredentials()` lazily, inline, only at the point Zoom is actually about to be called - after every cheap, non-Zoom check has already had the chance to return or throw first · a default parameter value in JS/TS is not "lazy until needed" - it evaluates unconditionally whenever the caller omits that argument, in left-to-right parameter order, before the function body runs. A function that does real permission/validation checks before its first real use of an expensive-or-fallible default should resolve that default INSIDE the function body, at the point of use, not as a default parameter value - especially when (as here) the whole point of making it a parameter was to let a test skip needing that dependency configured at all.
- 2026-09-04 · P7b verification (Stream B) · uptime monitoring (P7b's own second deliverable, alongside PR1's Sentry/PII-scrubbing) looked unbuilt from every angle a codebase search can check - no env var, no CI step, no code anywhere references an uptime service - and stayed on the "what's left in Stream B" list for a session as a result. It was already done: Ferenz confirmed a real UptimeRobot monitor already exists in Ivan's own account, polling `https://kinkeepers.vercel.app/api/health` (the P7a health-check endpoint, which already checks database/auth/Zoom together) every 5 minutes, alerting to Ivan's real inbox. Verified with actual evidence, not just his say-so: UptimeRobot's own Incidents tab showed two real production incidents that fired and resolved correctly (2026-08-29, a 500; 2026-09-02, a 503), and a deliberate TEST down→up cycle from 2026-08-29 that generated matching "TEST: Monitor is DOWN"/"TEST: Monitor is UP" emails - satisfying the acceptance line's own "verified, not assumed" bar for a simulated-outage alert. Documented at `docs/ops/uptime-monitoring.md` so it isn't lost or reconstructed as missing again · a feature that would normally leave a trace in the repository (an env var, a webhook config, a CI step) leaving NO trace is not proof it doesn't exist - it can mean the infrastructure was set up directly in a third-party account outside any session's own PR, which is exactly what makes it invisible to grep. Before listing something as unbuilt purely because a codebase search found nothing, especially anything that's fundamentally an external SaaS/ops concern (uptime monitoring, DNS, a status page, a paid API account), ask the person who might have set it up directly rather than trusting the absence of code as proof of absence.
- 2026-09-04 · UI/UX consistency pass, manual verification (Stream A) · with the admin-issued sign-in link fix live, signed in as a real facilitator and admin fixture on the hosted staging project to screenshot the authenticated screens the previous session's auth bug had blocked - all landed correctly, but manually navigating a signed-in facilitator to `/` (the caregiver home) surfaced a real dead-end: `components/ui/error-state.tsx`'s `not_found` variant hardcoded its "Go to Home" link to `href="/"`, unconditionally. `/` 404s for any non-member role (`getViewer()` calls `notFound()` when the signed-in user has no `applicants` row), so a facilitator or admin who lands on either not-found page (`app/(caregiver)/not-found.tsx` for a wrong-role visitor to `/`, or `app/facilitator/not-found.tsx` for the same case in reverse, per that file's own comment "also what a signed-in wrong-role visitor sees") would click "Go to Home" and land on the exact same dead-end page again, forever - a direct violation of CLAUDE.md's "error states never dead-end." This is the same family of bug as the 2026-09-02 X4 entry above (a shared default assuming the visitor is a member), but in the error UI rather than the sign-in redirect - that earlier fix only touched `/auth/callback`'s redirect target, never touched `ErrorState`'s independently-hardcoded home link. Fixed by giving `ErrorState` an optional `homeHref` prop (default `"/"`, unchanged for any caller that doesn't pass one) and having both not-found pages resolve it from the visitor's own role via the already-existing `getCurrentRole()`/`roleHomePath()` helpers. Verified live with Playwright against the hosted staging project (not just reasoned about): signed in as the real facilitator fixture, navigated to `/`, confirmed the rendered link's `href` was `/facilitator`, clicked it, and confirmed it actually landed on the real facilitator home rather than looping · a shared error/not-found component's own hardcoded fallback destination needs the same role-awareness audit as a redirect does - fixing one call site of "where does a signed-in user go by default" (a sign-in callback, say) does not mean every OTHER hardcoded assumption of "the visitor is a member" elsewhere in the codebase (an error page's home link, here) got fixed along with it. When auditing a role-visibility bug, grep for every other hardcoded `href="/"`/`redirect("/")` a non-member role could actually reach, not just the one already reported.
- 2026-09-04 · Production manual verification (Stream A) · Ferenz's real admin sign-in on the freshly cut-over production site (`kinkeepers.vercel.app`, Supabase project `vnadfnnckmkswfrzfjkj`/Kinkeepers-prod, created the day before) requested a magic link, received it, clicked it, and landed back on the sign-in-adjacent 404 - not a code bug, and not the PKCE/admin-link issue fixed earlier this session (this was a normal self-service `signInWithOtp` link, the `code`/`exchangeCodeForSession` branch that already works correctly on staging). Root cause, found via `vercel logs kinkeepers.vercel.app`: zero requests ever hit `/auth/callback`, meaning GoTrue itself never honored the redirect. Checked in the dashboard: production's Authentication → URL Configuration had Site URL still at the Supabase default `http://localhost:3000` and a completely empty Redirect URLs allowlist - R1's cutover created the new project but never carried over ANY of staging's Auth-level dashboard config, since none of that lives in a migration or in this repo at all. With an empty allowlist, GoTrue rejects every `redirect_to` the app sends and falls back to Site URL (`localhost:3000`), which explains both symptoms at once (no `/auth/callback` hit, and landing back on sign-in). Fixed by setting Site URL to `https://kinkeepers.vercel.app` and adding that domain's (plus its two Vercel alias domains') `/auth/callback` explicitly to Redirect URLs - deliberately NOT a wildcard like `https://*.vercel.app/auth/callback`, since that would also match preview-deployment URLs, which are wired to the *staging* Supabase project, and blurring that separation defeats the point of having two projects. Fixing this surfaced a second, independent gap immediately: the now-successful sign-in landed the admin on `/` (the member home) instead of `/admin`, 404ing again - because `handle_new_user()`'s trigger (`supabase/migrations/20260826183733_profiles_and_sign_in_events.sql`) inserts every new `profiles` row with no role specified, taking the column default `'member'`, and `requestEmailLink()`'s `signInWithOtp` call never sets `shouldCreateUser: false` - so simply typing an email into sign-in silently creates a brand-new user on whatever Supabase project has never seen that email before. Since production's `auth.users` table was completely empty post-cutover, this was literally that admin email's first-ever sign-in on the new project, and it got the default `member` role - there is no self-service path to admin in this app by design (role resolves server-side from the database only, per CLAUDE.md invariant #9), and neither `seed.sql` nor any migration ever grants `role = 'admin'` to anyone; staging's own admin account was elevated by a one-off manual `UPDATE`, undocumented anywhere as a repeatable step. Fixed the same way, by hand, directly against production: `update profiles set role = 'admin' where id = (select id from auth.users where email = '<admin email>')`. A third gap surfaced right after: production's magic-link emails came from Supabase's own default sender (`noreply@mail.app.supabase.io`), not "KinKeepers" - because Supabase Auth's own outbound email (magic links, OTP) is sent by GoTrue using the project's own SMTP settings, entirely separate from the app's `RESEND_API_KEY`/`RESEND_FROM_EMAIL` (used only for the app's OWN transactional sends - reminders, notifications). `supabase/config.toml`'s own `[auth.email.smtp]` block already had the correct, working values (Resend, `sign-in@signalpath.brandlamb.com`, sender name "KinKeepers" - the same setup already live on staging), but `supabase config push` was deliberately NOT used to apply it: that command pushes the ENTIRE config.toml with no scoping flag, and this file's `[auth]` section still carries `site_url = "http://127.0.0.1:3000"` and `additional_redirect_urls = ["https://127.0.0.1:3000"]` for local dev - running it against production would have silently clobbered the URL Configuration fix made minutes earlier. Fixed by setting only the SMTP fields by hand in the dashboard instead, copied from config.toml's own values · cutting a Supabase project over to a new one (R1-style, or any future re-provisioning) carries NONE of the following automatically, and none of it is captured in a migration or anywhere in this repo: Auth URL Configuration (Site URL, Redirect URLs allowlist), custom SMTP settings, and any human's elevated role (admin/facilitator/etc., since nothing in seed.sql or any migration ever grants one). All three are dashboard-only state, invisible to `supabase db diff`, `db reset`, or any test suite, and only surface when a real human actually tries to sign in for the first time against the new project - exactly the failure mode the 2026-09-01 A3 "video_occurrence_id" and P4/A5 "sourceless Vercel" entries above already describe for other kinds of drift between what's committed and what's actually configured. Also: never run `supabase config push` against a project whose dashboard-configured URL settings differ from local dev's config.toml (which will almost always be true, since config.toml's `site_url`/`additional_redirect_urls` are meant for `localhost`) - it pushes the whole file with no way to scope it to one section, so it can silently revert unrelated, already-correct production settings as a side effect of applying an unrelated one. The next new-Supabase-project cutover needs a written checklist covering these three items specifically, since none of them announce their own absence until a real sign-in fails.
- 2026-09-04 · A4-payouts / F3 unblocking (Stream A) · asked what actually blocks the two parked sessions, traced "B1"/"B3" through every planning document available: neither label appears in `trib4l-build-from-zero.md` v2.1 (which builds org-as-merchant Stripe Connect commerce in Sessions 13–16 and platform revenue-ops in Session 19, never individual mentor/facilitator compensation), nor in the F4milia Complete Run Doc (which states outright "commerce stays dormant-per-Tower and nothing in this doc touches it"), and `f4milia-revenue-model.md` itself flags mentor payouts as "new scope beyond what Session 9 or Session 16 currently describe... worth resolving which session owns it" — a question no later document answered. So A4-payouts and F3 were parked on a dependency that was never scheduled by anyone, in any version of any plan, and the run doc's own "confirm status by end of Wave 5" checkpoint for them was never performed. Also found that the premise didn't hold structurally: KinKeepers runs its own Supabase project, so even if F4milia had built `mentor_agreements`/`mentor_payouts`, no KinKeepers screen could have read them. Meanwhile the inputs for a per-session facilitator payout already exist here — `session_logs.delivery_confirmed` (X4) as the verified-delivery record and `sessions.substitute_facilitator_id` (A3), whose migration comment says verbatim "base is earned per session delivered, by whoever delivered it." Response: wrote B1 and B3 as full session prompts (run doc, PARKED section, marked PROPOSED) scoped to this repo's schema, with the four business decisions Ivan must record first (per-session-only vs revenue share — `f4milia-revenue-model.md` §2 and F3's own prompt text contradict each other on this; what "release" does; payout period; rate per facilitator vs per program) and with B2 (money movement) explicitly not written · a "parked, blocked on X" entry is a claim that X is scheduled somewhere; check that it actually is before treating the block as someone else's problem — here it wasn't, and the checkpoint meant to catch that was skipped. When a parked feature's dependency is described as living on another system, verify the two systems can actually see each other's data before assuming the dependency is even the right shape.
- 2026-09-04 · P1 strict acceptance-criteria audit (Stream A) · re-testing P1's literal acceptance line ("sign in by email link... end to end") against a real local sign-in, not assuming it from the admin-link fix or from pgTAP/vitest passing, surfaced two real, previously-undiscovered local-dev bugs and one confirmed-clean-now scare. First: `supabase/config.toml`'s `[auth]` `site_url`/`additional_redirect_urls` held only bare origins with no `/auth/callback` path at all (and the wrong protocol - `https` on a local `http` server) - `requestEmailLink()`'s real `redirect_to` (`<origin>/auth/callback`) matched neither entry, so GoTrue silently substituted `site_url` instead of honoring it, landing the browser on `/` with the `code` parameter never reaching `/auth/callback` at all and no error shown anywhere - a completely silent failure. Second, found while chasing why Mailpit wasn't capturing the test email at all: `[auth.email.smtp]`'s `enabled = true` is not scoped to the hosted project the way its own comment implies - `supabase start` applies it to local GoTrue too, so local dev was actually sending every magic-link email through real Resend instead of the local Mailpit catch-all, confirmed via the local GoTrue container's own logs showing a real Resend rejection (`550 "Invalid 'to' field... example.com"`) for a plain test address. This directly contradicted `docs/qa/FIXTURES.md`'s documented claim that local sign-in "always routes through Mailpit regardless of the email's real deliverability." Both fixed in config.toml (added the correct `/auth/callback` redirect URLs for both `127.0.0.1` and `localhost`; set `enabled = false` on the SMTP block, keeping its values as a record of what the hosted dashboard should have, not something `supabase config push` should ever apply - see the entry above on why that command is dangerous to run against a configured hosted project). Verified end to end with a real Playwright-driven browser through the actual `/sign-in` form and a real Mailpit-captured link, both before (silent failure reproduced) and after (correct sign-in, and a second click on the same link correctly rejected as single-use) each fix. The scare: `getCurrentApplicantOrNotFound()`'s `claim_applicant_for_current_user` RPC threw a real `permission denied for function` error during this same investigation - alarming because the identical error had appeared in real production logs earlier this session - but reproducing it a second time after a genuine `supabase db reset --local` (rather than a `supabase stop`/`start` cycle, which restores from a Docker-level backup snapshot instead of replaying migrations) made it vanish; the grant is present and correct in `20260901190000_member_identity_bridge.sql`, and a fresh reset produces the correct behavior (an honest not-found for a brand-new signup with no applicant to claim, not a permission error). The production occurrence, checked again, has not recurred in the 12 hours since it was first observed - most likely a transient artifact of the same freshly-cut-over-project period as the other production Auth gaps found earlier this session, not an ongoing defect, though this is not a certainty and it's worth a second look if it ever recurs. Separately documented (README, not a code fix - there is nothing to fix): local sign-in testing must use `http://localhost:3000` consistently, not `http://127.0.0.1:3000` - `next dev` itself normalizes `/auth/callback`'s computed redirect origin to `localhost` regardless of which host the request actually arrived on, so mixing the two silently drops the session cookie between the callback's redirect and the next request. Confirmed this is `next dev`-specific, not a real defect: production's own real self-service sign-in (verified by Ferenz earlier this session) has no such ambiguity, since a real deployment serves one canonical domain · a passing test suite and a working ADMIN-issued link are not evidence that the ordinary, most-common path (a real user requesting their own magic link) actually works - this session had strong indirect signals it should work (pgTAP green, vitest green, the admin-link fix's own verifyOtp test passing) and none of them would have caught either config.toml bug, because neither is exercised by any automated test; both are dashboard/config-adjacent state exactly like the production cutover gaps found earlier today. When auditing an acceptance criterion phrased as "X works end to end," the only real evidence is driving the actual UI through the actual flow a real user would use, start to finish, in a fresh environment - not the adjacent code paths that happen to share the same underlying primitive. Also: `supabase stop` followed by `supabase start` restores local Postgres from a backup snapshot, not from a clean migration replay - it can mask or fabricate schema-state bugs that a real `supabase db reset --local` would not; treat any surprising local-only error as suspect until reproduced (or not) after a genuine reset, the same discipline already established for ruling out Docker flakiness.
- 2026-09-04 · P2 strict acceptance-criteria audit (Stream A) · re-verifying P2's own acceptance line ("referral link and staff form both create records") against the real app found that the staff-facing referral path - "a care navigator at a partner organization... submits on the caregiver's behalf," explicitly one of P2's two required referral paths - had a fully-built, fully-tested Server Action (`createStaffReferral()`, its own describe block in `lib/referral/actions.test.ts` covering source attribution, role rejection, and unauthenticated rejection) but no screen anywhere that called it. Confirmed by grep (zero references to the function outside its own test file and definition) and by `lib/admin/nav.ts`, whose own header comment quotes A1's persona spec ("partner staff see only 'Cohorts' and 'Reports'") - a spec written in Wave 2, one wave AFTER P2 shipped in Wave 1, that simply never accounted for the referral screen P2's own prompt required. Same shape as the A5/A4-cert "session shipped before its dependency, nothing ever came back to finish the wiring" pattern already in this log twice. A real partner-organization user had no way to refer anyone through the app at all, despite the underlying data model, RLS, and tests all being correct and complete. Fixed by building `app/admin/refer` (gated to `partner_staff`, matching the action's own role check - not `admin`, which has no `partner_organization_id` to scope a referral to), a new `createStaffReferralAction` form-state wrapper (`lib/referral/actions.ts`, reusing the already-exported `getRequestOrigin()` rather than duplicating host-derivation logic), and a "Refer someone" nav entry. Verified end to end live, not just via the existing unit tests: created a real `partner_staff` fixture, signed in, submitted a referral with a `partner_reference_id`, confirmed the resulting DB row's `referral_source`/`partner_reference_id`/org scoping, and followed the shown resume link to a real, working intake form · a Server Action with full test coverage is not evidence a feature is reachable - grep for the UI actually calling it before treating "the backend is built and tested" as "the feature exists." When one wave's own nav/persona spec is written by a LATER wave than the session whose feature it's supposed to expose, check whether that later wave's spec actually accounted for everything the earlier one required, the same way a later wave's own comment can leave an earlier wave's deferred item permanently stranded (see the 2026-09-03 A5 gap-closure entry above for the same shape, in the other temporal direction).
- 2026-09-04 · P1 audit follow-up (Stream A) · checking P1's "session survives a 30-day gap" acceptance atom against the real dashboard (not assumed from the 2026-08-26 entry recording that this was once set correctly) found a fourth instance of the same R1-cutover gap: production's Authentication → Sessions inactivity timeout was still at the Supabase default, never set to the spec's 90 days (2160 hours) - staging already had the correct value. Ferenz set production to 2160 hours directly. This is the same shape as the three gaps in the "Production manual verification" entry above (URL Configuration, admin role, custom SMTP) - another piece of Auth dashboard state that a new Supabase project does not inherit from the old one and that lives nowhere in this repo · the written cutover checklist that entry called for needs a fourth line: session inactivity timeout. More generally, for THIS specific setting: the 2026-08-26 entry about `sessions_inactivity_timeout` expecting hours not seconds describes a mechanical unit-conversion mistake, not a guarantee the value is actually set correctly on every project going forward - a new project starts at Supabase's own default regardless of what a past incident taught about the field's units, and needs the value applied again, explicitly, every time.
- 2026-09-04 · A2 strict acceptance-criteria audit (Stream A) · re-verifying A2's own review-queue requirement ("List of applicants in pending_review with their intake data: first name, relationship, care recipient stage, time zone, availability, referral source, days waiting") found `availability_windows` - a real column, collected during intake, stored correctly (confirmed via `lib/referral/actions.ts`'s `saveIntakeProgress`) - was never selected or displayed anywhere: not on the review queue list, not on the assignment/detail page, not anywhere in `lib/admin/applicants.ts`'s `QueuedApplicant`/`ApplicantDetail` types at all. A reviewer deciding which open cohort's meeting time would actually work for someone had no way to see when that person said they were free, despite A2's own spec listing availability as one of exactly seven required review-queue fields and despite the assignment screen already correctly showing cadence/meeting-time-in-the-applicant's-zone for the OTHER half of that same judgment call. Fixed by adding `availabilityWindows: string[]` to the shared applicant types, selecting the column in all three query functions, and rendering it on both screens using the intake form's own existing `COPY.referral.availability_option` labels (no new copy invented). Verified live, not just via the added vitest assertions: created a real applicant with real availability values, confirmed the exact rendered text on both the queue and detail pages via a real signed-in admin session. Everything else in A2 (queue ordering, RLS-backed data scoping already proven in A1's own audit, assignment writing status/event/audit and removing the applicant from the queue, the waitlist summary view, decline reason codes, and a clean grep for any matching/scoring logic) was already correctly built and well-tested · a screen showing "most of" a required field list can pass a casual look and still be silently missing one - check every named field against the actual query/type, not just against what visibly renders when skimming the page, especially for a field (availability) that has no other UI surface anywhere to cross-check it against.
